.title_tools
  = link_to_inline(:create_task, url_for(:action => :new, :view => @view), :text => t(:create_task))
  //= link_to(t(:create_task_small), new_task_path + "?view=#{@view}", :remote => true)
  //= link_to(t(:create_task), new_task_path + "?view=#{@view}", :data => { no_turbolink: true }, :remote => true)
.title#title
  = (@view == "assigned" ? t(:assigned_tasks) : t(:tasks))
  = image_tag("loading.gif", :size => :thumb, :id => "loading", :style => "display: none;")

.remote#options{ hidden }
.remote#create_task{ hidden }
